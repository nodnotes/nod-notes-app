-- Frontend-only marker (no schema change).
-- Host minimap mirrors selected nested preview (pan/fit); wheel zoom stays on host board;
-- fixed map chrome stacks above portaled preview shell.
select 1;
