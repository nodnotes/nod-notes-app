-- Frontend-only marker (no schema change).
-- Brand mark: board-fill disc + grey border; default T/dot black (light) / white (dark).
-- Applies to map chat toggle + chat sidebar (`Nod NotesBrandMark` discVariant board default).
-- Schema unchanged.
select 1;
