-- Frontend-only marker (no schema change).
-- Nested board preview mirrors host top-bar tools (draw, scroll/zoom nav, select/pan)
-- inside the preview iframe; preview chrome wheel still zooms the host board.
-- Notion sync delete dialog, restore-archived API, and Nod Notes-only hidden rows.
select 1;
