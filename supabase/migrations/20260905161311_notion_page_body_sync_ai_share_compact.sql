-- Frontend-only marker (no schema change).
-- Notion imported page body live-sync Nod Notes → Notion (debounced PUT);
-- background Notion → Nod Notes detect (notionUpdatesPending); remove Manual sync mode;
-- AI sparkles fold into board More on shareCompact; chat header/empty-state width;
-- prompt bar width tracks chat column resize.
select 1;
