-- Frontend-only marker (no schema change).
-- Nested preview: host top-bar tools only when selected; drag moves host frame
-- without selecting (like an unselected frame); auto-select on open.
select 1;
