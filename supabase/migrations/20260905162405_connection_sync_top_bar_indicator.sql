-- Frontend-only marker (no schema change).
-- Top-bar sync icon left of Connections pin (blue when notionUpdatesPending);
-- optimistic React Query patch for pending Notion → Nod Notes updates.
select 1;
